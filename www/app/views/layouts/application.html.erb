<!DOCTYPE html>
<html>
<head>
  <title>Chorechart</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/default/style", "web-app-theme/override", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <header>
      <h1><a href="/">Chorechart</a></h1>
      <nav id="user-navigation">
        <%= render 'shared/user_info' %>
      </nav>
    </header>    
    <div id="wrapper" class="wat-cf">
      <div id="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <div class="block">
          <div class="content">
            <%= yield %>
          </div>
        </div>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> <a href="http://github.com/theunraveler">theunraveler</a></p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
        <% if user_signed_in? %>
          <%= render 'shared/user_chores' %>
        <% end %>
      </div>
    </div>    
  </div>
</body>
</html>
