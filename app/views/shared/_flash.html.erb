<% flash.each do |type, message| %>
  <div class="alert-message block-message <%= flash_type(type) %> fade in" data-alert="alert">
    <a class="close" href="javascript:void(0);">x</a>
    <p><%= message %></p>
  </div>
<% end %>
