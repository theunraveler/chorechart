/*
*= require bootstrap/bootstrap
*= require jquery_ui/aristo
*= require slimbox/slimbox2
*= require_self
*/

.print { display: none; }
.clearer { clear: both; }

.icon {
  background-image: url(<%= asset_path 'icons.png' %>);
  width: 64px;
  height: 64px;
  display: block;
  text-indent: -9999px;
}
.icon.inline { display: inline-block; }
.icon.github { background-position: 0px -64px; }
.icon.facebook { background-position: 0px 0px; }
.icon.google_oauth2 { background-position: 0px -128px; }
.icon.openid { background-position: 0px -192px; }
.icon.twitter { background-position: 0px -256px; }
.icon.print { background-position: 0px -320px; width: 16px; height: 16px; }
.icon.print:hover { background-position: 0px -336px; }
.icon.info { background-position: 0px -352px; width: 16px; height: 16px; }
.icon.info:hover { background-position: 0px -368px; }

.brand:before {
  content: "";
  background: url(<%= asset_path 'icons.png' %>) 0px -384px;
  width: 17px;
  height: 16px;
  float: left;
  margin: 3px 5px 0 0;
}

th small { color: #BFBFBF; }

.secondary-nav .user-thumb { padding-right: 7px; }
.secondary-nav.no-user-menu { margin: 8px 8px 0 0; color: #909090; }
.secondary-nav.no-user-menu a { display: inline; }

.page-header .pull-right { margin-top: 5px; }
.page-header .pull-right.icon { margin-top: 14px; }

.btn-group .btn.btn-disabled { color: #999; cursor: default; }

#chore-breakdown .user-thumb { padding-right: 10px; padding-top: 2px; }

#group-memberships .invitation td { font-style: italic; color: #808080; }

#my-chores .label a { color: #FFFFFF; }
#my-chores div.nav-list { padding: 0 0 5px 0; }

section.feature:not(:first-child) { padding-top: 60px; }

img.bordered { border: 1px solid #DDD }

/*************************
* SPEECH BUBBLES
*************************/
.speech-bubble {
  background-image: -moz-linear-gradient(top , #049CDB, #0064CD);
  background-image: -webkit-linear-gradient(top , #049CDB, #0064CD);
  background-image: linear-gradient(top , #049CDB, #0064CD);
  background-repeat: repeat-x;
  border-color: rgba(0, 0, 0, 0.1) rgba(0, 0, 0, 0.1) rgba(0, 0, 0, 0.25);
  color: #FFFFFF;
  text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.25);
  border-radius: 10px 10px 10px 10px;
  margin: 0 0 1em;
  padding: 15px;
  position: relative;
}
.speech-bubble:after {
  border-color: #0064CD transparent;
  border-style: solid;
  border-width: 20px 0 0 20px;
  bottom: -20px;
  content: "";
  display: block;
  position: absolute;
  width: 0;
}
.hero-unit .speech-bubble:first-child { margin-top: 10px; }
.speech-bubble.left { float: left; }
.speech-bubble.left:after {
  border-width: 20px 20px 0 0;
  left: 50px;
}
.speech-bubble.right { float: right; }
.speech-bubble.right:after {
  border-width: 20px 0 0 20px;
  right: 50px;
}

/*******************************
* OVERRIDES FOR BOOTSTRAP ICONS
*******************************/
[class^="icon-"], [class*=" icon-"] {
  background-image: url(<%= asset_path 'bootstrap/glyphicons-halflings.png' %>);
}
.icon-white {
  background-image: url(<%= asset_path 'bootstrap/glyphicons-halflings-white.png' %>);
}

/*******************************************************
* FIX FOR LINE BREAKS IN LABELS (WILL BE FIXED IN 2.0.2)
 *******************************************************/
.label { white-space: nowrap; }
